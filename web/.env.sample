# URL of the mite-mite API (e.g. http://localhost:4100)
VITE_API_URL=http://localhost:4100

# Admin secret — must match ADMIN_SECRET in api/.env.
# When set, admin-only UI (Add Entry, etc.) becomes visible and accessible.
VITE_ADMIN_SECRET=
