query GetFranchises($search: String) {
  franchises(search: $search) {
    id
    primaryTitle
    primaryEntryId
    entries {
      id
      coverImageUrl
      genres
      tags
      status
    }
  }
}
