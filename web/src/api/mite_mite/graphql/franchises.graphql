query GetFranchises(
  $search: String
  $genres: [String!]
  $tags: [String!]
  $status: String
  $medium: Medium
) {
  franchises(search: $search, genres: $genres, tags: $tags, status: $status, medium: $medium) {
    id
    primaryTitle
    entries {
      coverImageUrl
    }
  }
}
