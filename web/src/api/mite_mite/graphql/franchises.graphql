query GetFranchises($search: String, $genres: [String!], $tags: [String!], $status: String) {
  franchises(search: $search, genres: $genres, tags: $tags, status: $status) {
    id
    primaryTitle
    entries {
      coverImageUrl
    }
  }
}
