query GetFranchiseDetails($id: ID!) {
  franchise(id: $id) {
    id
    primaryTitle
    primaryEntryId
    entries {
      alternateTitles
      comments
      coverImageUrl
      description
      genres
      id
      medium
      metadata
      primaryTitle
      referenceUrl
      staff
      status
      tags
    }
  }
}
